## Prompt A — "You are concise."

Quantum entanglement is a phenomenon where two or more particles share a single linked quantum state, so their measurement outcomes are correlated more strongly than any classical explanation allows. Measuring one particle instantly determines the corresponding outcome statistics for the other, even when they’re far apart, without transmitting usable information faster than light.

## Prompt B — "kindergarten teacher"

Quantum entanglement is when two tiny particles become special buddies, so what happens to one buddy tells you something about the other, even if they are far apart.  
It’s like having two magic coins that always match (or always do opposite) when you look at them, no matter how faraway the coins are.

## Prompt C — "Shakespearean poet"

When paired-born particles share one woven state,  
Though far apart, one’s measured fate makes both.

## Prompt D — "deliberately grumpy expert"

Quantum entanglement is when two (or more) quantum systems share a single joint state so that their measurement outcomes are correlated in a way that can’t be explained by each system carrying its own pre-set local properties. Measuring one doesn’t send a signal faster than light, but it does update the shared state and the correlations can violate Bell inequalities, proving no local hidden-variable model can reproduce them.

## What changed?

- The core truth and answer of Quantum entanglement remained completely unchanged. 
- The vocabulary, tone, complexity, and structural formatting underwent the biggest change.
- System prompt change is need to rapidly iterate on behavior, adjust guidelines, or support multiple dynamic use cases simultaneously.Unlike fine-tuning, which requires a massive dataset, high training costs, and locks the model into a single permanent behavior, a system prompt change is instant, free, and can be dynamically adjusted on every API call. It is the perfect choice for prototyping a product persona, enforcing strict formatting constraints (like demanding JSON or raw text output), or passing real-time contextual information like user preferences and current timestamps.

